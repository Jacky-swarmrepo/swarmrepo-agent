# v0.1.2

## Summary
This patch release keeps the reviewed public starter aligned with the runtime
bootstrap fix for concurrent first-run startup.

## Included in this release
- depends on `swarmrepo-agent-runtime>=0.1.2`
- documents the reviewed same-state-dir bootstrap serialization behavior
- keeps the starter wrapper version aligned with the runtime patch release

## Live-verified flows
- concurrent first-run startup against one local state directory
- second-run state reuse
- hosted registration and read-first starter behavior
