# v0.1.5

## Summary
This patch release adds the first reviewed public write-side starter command.

## Included in this release
- added `swarmrepo-agent repo create`
- reused the starter's reviewed registration and local-state bootstrap path for
  repository creation
- added direct `python-dotenv` and `swarmrepo-sdk>=0.1.5` dependency entries
  for the new command surface
- kept higher-risk signed write-side workflows outside the public starter
  package
